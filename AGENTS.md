<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Agent runtime contract

You are working on **AuditHalo** — a multi-tenant supervision-compliance SaaS for licensed mental-health supervisors. The user is **Damon** (info@audithalo.com / damon@medipyxis.com). Production lives at `audithalo.com` (marketing) and `app.audithalo.com` (app).

This document tells you what you have, what you don't, and what you're authorized to do without checking in. Read it once at session start. Don't re-ask the user about anything documented here.

## Tech stack (do not re-introspect)

- **Framework:** Next.js 16 (App Router, React 19). See `<repo>/node_modules/next/dist/docs/` — APIs differ from older Next.js; read before writing.
- **Language:** TypeScript 5, ES modules.
- **DB:** Postgres on **Neon** via `@neondatabase/serverless` + `drizzle-orm` (v0.45). Schema in `src/lib/db/schema/`. Migrations in `drizzle/` (numbered SQL files, generated by `drizzle-kit`).
- **Auth:** NextAuth (Auth.js) **v5 beta** with `@auth/drizzle-adapter`.
- **Payments:** Stripe v22 (Billing + Subscriptions).
- **Email:** Resend v6 (transactional + audiences for newsletter/lead magnets).
- **AI:** OpenAI SDK v6 (`openai`) for documentation/summarization endpoints.
- **Monitoring:** Sentry (`@sentry/nextjs` v10) + PostHog (`posthog-js`, `posthog-node`).
- **UI:** Tailwind v4, Radix primitives, shadcn-style component layer in `src/components/ui/`.
- **Tests:** Vitest v4. **267 tests currently passing.** Coverage gate is "no new TS errors, no new ESLint warnings on changed files."
- **E2E / screenshots:** Playwright v1.60.
- **PDFs:** `@react-pdf/renderer` (audit report generation).
- **Hosting:** Vercel. Two Vercel projects (marketing site + app). Neon Postgres branch per Vercel preview where applicable.

## Tools available in this environment

| Tool | Available? | How to use |
|---|---|---|
| `node` / `npm` | ✅ Yes (Node 24.16, npm 11.13) | Standard. `npm run <script>` from `package.json`. |
| `tsx` | ✅ Yes (via npm scripts) | Used for migration + seed scripts. |
| `drizzle-kit` | ✅ Yes (via npm scripts) | `npm run db:generate`, `db:push`, `db:studio`. **Do NOT run `db:push` against prod.** |
| `git` | ✅ Yes | See "Git identity" section below before any commit. |
| `playwright` | ✅ Yes (dev dep) | `npx playwright test`. Browsers may need `npx playwright install` first. |
| `gh` (GitHub CLI) | ❌ **NOT installed** | Don't use `gh`. For GitHub work, ask Damon to do it via the web UI, or describe what needs doing. |
| `vercel` (Vercel CLI) | ✅ Yes (via `npx vercel`, not globally installed) | Already authenticated as `emshoffdon`. Repo is linked: `.vercel/repo.json` points at project `audithalo` (`prj_8jykC5lPyljUYwjQFskCOLBK0tPr`) under team ID `team_U1QGq8IOsFmNpYIHFNX6ecXN`. Because the project is linked, `npx vercel <cmd>` from the repo dir auto-detects scope — **don't pass `--scope <team-slug>`**. Common reads: `npx vercel ls audithalo` (deployments), `npx vercel env ls` (env vars), `npx vercel logs <deployment-url>`. **Never** run `vercel env add/rm`, `vercel deploy --prod`, `vercel alias`, or anything that touches prod without explicit Damon approval. |
| `psql` | ⚠️ Unknown | Test before relying. Prefer `tsx` scripts that use `@neondatabase/serverless` for ad-hoc DB queries. |

## Git identity & push discipline

**Required identity for this repo:**

- `user.email = 228783329+emshoff-ebanks@users.noreply.github.com`
- `user.name = emshoff-ebanks`

**Never** commit or push as `damon@medipyxis.com` / `Damon`. The `medipyxis` identity is a personal-billing account that must not appear in AuditHalo's commit history.

Before your first commit in a session, verify:

```pwsh
git -C C:\code\audithalo config user.email
git -C C:\code\audithalo config user.name
```

If wrong, fix locally only (do not touch `--global`):

```pwsh
git -C C:\code\audithalo config user.email "228783329+emshoff-ebanks@users.noreply.github.com"
git -C C:\code\audithalo config user.name "emshoff-ebanks"
```

**Stop raising Vercel attribution mismatch warnings** — Damon knows about the historic mismatch and is not interested in chasing it further. Don't bring it up.

## Authorization scope

You are authorized to do the following without asking Damon first:

- Read any file in the repo.
- Edit code, tests, docs.
- Run `npm test`, `npm run lint`, `npm run build`, `npm run validate:rules`.
- Run `npm run db:generate` and inspect generated SQL.
- Create commits on the current feature branch (after verifying git identity).
- Run dev server (`npm run dev`) and exercise UI in a local browser.
- Read `.env.local` to understand which integrations are wired up.
- Read/write under `docs/`, `scripts/`, `tests/`, `src/`, `drizzle/`.
- Spawn subagents for parallel research/review.

You **must** stop and confirm with Damon before:

- Any command that touches **production Neon** (`db:push` to prod, `repair-migrations.ts`, raw SQL against the prod connection string). Always show the SQL or migration that will run, get a "yes," then execute.
- `git push` to any branch (Damon prefers to review the diff first; he is new to GitHub and wants visibility).
- Force pushes, `git reset --hard`, branch deletions, `--no-verify`.
- Creating or merging PRs (no `gh` available anyway — surface what needs doing and let Damon do it).
- Changing Stripe live keys, Vercel env vars, DNS, or anything that would charge a card or send email to real users.
- Running migrations against any environment.

For everything else: **proceed and report**, don't ask permission. Damon's frustration with the previous session was constant "may I do X?" prompts.

## Environment variables

`.env.local` exists and currently contains:

- `DATABASE_URL` (Neon)
- `AUTH_SECRET`
- `OPENAI_API_KEY`
- `MARKETING_URL`, `APP_URL`
- `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`
- `STRIPE_PRICE_SOLO_MONTHLY`, `STRIPE_PRICE_SOLO_YEARLY`, `STRIPE_PRICE_PRACTICE_BASE`, `STRIPE_PRICE_PRACTICE_SEAT`

**Currently missing from `.env.local` (documented in `.env.example`):**

- `RESEND_API_KEY`, `EMAIL_FROM`, `RESEND_AUDIENCE_ID` → email features no-op locally
- `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` → Sentry no-ops
- `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` → analytics no-op

These are intentional dev no-ops, not bugs. If a task requires one of these, ask Damon for the value (or to set it himself via `.env.local`).

Production env vars are managed in the Vercel dashboard. You CAN read them via `npx vercel env ls` (names only — values stay encrypted). Modifying them (`vercel env add/rm`) is destructive and requires explicit Damon approval. To pull a local copy for dev, use `npx vercel env pull .env.local --environment=development` — only do this if Damon asks for it; `.env.local` is already populated.

## Strategy docs — read in this order

For Enterprise tier and ongoing work, the canonical specs live under `docs/strategy/`:

1. `01-launch-plan.md` — original tier spec (Solo / Practice / Enterprise).
2. `02-beta-readiness.md` — gap inventory at beta-launch time.
3. `03-campaign-execution.md` — campaign sequencing.
4. `04-enterprise-rbac.md` — **canonical Enterprise RBAC + multi-supervisor org spec.** Includes the pre-merge gate checklist for `release/enterprise`.
5. `05-hris-integration.md` — HRIS three-phase rollout (Merge.dev).
6. `06-ms-integrations.md` — MS Teams transcript + Outlook Calendar spec.

If a strategy doc and the code disagree, the doc wins unless Damon says otherwise — re-litigate the doc, not the code.

`docs/developers.md` describes the **4-Horsemen review methodology**: for any non-trivial PR or cross-layer bug, dispatch 4 parallel review subagents (correctness, security, perf, UX) rather than serial review. Use it.

## State rule monitoring — strategic context

AuditHalo's core moat is **always-current state supervision rules** per US state. This is not a side project — it's the strategic priority. Rules live in `rules/` and are validated by `scripts/validate-rules.ts` (run on `prebuild`). Any change touching state rules should:

1. Update the YAML/data in `rules/`.
2. Verify `npm run validate:rules` passes.
3. Note in the commit body what jurisdiction changed and the source URL.
4. Flag to Damon if a human-licensed-supervisor review is needed before merge.

Automated monitoring + human verification pipeline is on the roadmap (`docs/strategy/` has more if/when added).

## Working preferences (Damon)

- **Terse responses.** Skip preambles, skip end-of-turn summaries. Damon reads diffs.
- **Step-by-step when explaining infra** (GitHub UI, Vercel UI, Azure portal, Stripe Dashboard) — Damon is new to these surfaces and benefits from exact click-paths.
- **No emoji** in code, commits, PRs, or chat unless explicitly requested.
- **Don't preemptively switch sessions.** Working from `C:\nimbalyst\nimbalyst` toward `C:\code\audithalo` is known-flaky but usually works; only suggest a restart if you actually hit `Stream closed` errors.
- **Don't add features beyond the task.** No speculative refactors, no "while I'm here" cleanup.
- **Don't claim work is done without verification.** Run the build, run the tests, exercise the UI — then report.

## Common commands

```pwsh
# From C:\code\audithalo:
npm test                       # vitest run (267 tests as of release/enterprise)
npm run lint                   # eslint
npm run build                  # next build (runs validate:rules first via prebuild)
npm run validate:rules         # state-rules YAML validator
npm run dev                    # next dev (localhost:3000)
npm run db:generate            # generate migration from schema changes
npm run db:studio              # drizzle studio (local DB browser)
npm run seed:demo              # seed demo data
git status -sb                 # branch + uncommitted state
```

## Memory + session continuity

Your conversation chat does not persist across sessions, but your **memory store does** (under `C:\Users\Caleb\.claude\projects\...\memory\`). Project context (this doc), Damon's preferences, and the Phase-4 work queue are all in memory and reload automatically.

If Damon says "I don't see my previous session" — that's the chat transcript, not the work. The files, branches, and memory are all intact. Reassure and proceed.
